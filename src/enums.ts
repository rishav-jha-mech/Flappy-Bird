export enum BackgroundType {
  DAY = "day",
  NIGHT = "night",
}

export enum PipeType {
  RED = "red",
  GREEN = "green",
}

export enum BirdType {
  YELLOW = "yellow",
  RED = "red",
  BLUE = "blue",
}

export enum BirdMovement {
  UP_FLAP = "upflap",
  MID_FLAP = "midflap",
  DOWN_FLAP = "downflap",
}

export enum DifficultyLevels {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}
